import React from 'react';
import styles from '../../styles/components/Navigation.module.scss';

function Navigation() {
	return (
		<div >
			<p className={styles.textcolor}>navbar</p>
		</div>
	);
}

export default Navigation;